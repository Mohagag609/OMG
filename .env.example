# ===========================================
# 🗄️ إعدادات قاعدة البيانات الموحدة
# ===========================================

# نوع قاعدة البيانات (sqlite, postgresql-local, postgresql-cloud)
DATABASE_TYPE="postgresql-cloud"

# رابط قاعدة البيانات للبناء (مطلوب لـ Prisma)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# ===========================================
# 📁 SQLite (محلي - للتطوير السريع)
# ===========================================
SQLITE_DATABASE_URL="file:./dev.db"

# ===========================================
# 🏠 PostgreSQL محلي
# ===========================================
POSTGRESQL_LOCAL_DATABASE_URL="postgresql://postgres:password@localhost:5432/estate_management"

# ===========================================
# ☁️ PostgreSQL سحابي (Neon)
# ===========================================
POSTGRESQL_CLOUD_DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require&channel_binding=require"

# ===========================================
# 🔐 إعدادات الأمان
# ===========================================
JWT_SECRET="your-super-secret-jwt-key"
NEXTAUTH_URL="https://your-app-name.netlify.app"
NEXTAUTH_SECRET="your-nextauth-secret-key"

# ===========================================
# ⚙️ إعدادات التطبيق
# ===========================================
NODE_ENV="production"

# ===========================================
# 📊 إعدادات إضافية
# ===========================================
BACKUP_RETENTION_DAYS=30
BACKUP_SCHEDULE="0 2 * * *"
HEALTH_CHECK_INTERVAL=30000
LOG_LEVEL="info"
DATABASE_POOL_SIZE=10
QUERY_TIMEOUT=30000